void findup(string s){
    unordered_map<char,int> mpp;
    for(int i=0;i<s.size();i++){
        mpp[s[i]]++;
    }
    for(auto it:mpp){
       if(it.second>1){
          cout<<it.first<<"->"<<it.second<<endl;
       }
    }
}
